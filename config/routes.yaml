frontend-base:
    path: /
    controller: App\Controller\AppController::frontend
    methods: GET|HEAD
frontend:
    path: /{path}
    controller: App\Controller\AppController::frontend
    methods: GET|HEAD
    requirements:
        path: login|interests|"^(?!public).+"

statusCheck:
    path: /check-ticket/{token}
    controller: App\Controller\AppController::checkStatus
    methods: GET|HEAD

backend:
    path: /{path}
    controller: App\Controller\AppController::backend
    methods: GET|HEAD
    requirements:
        path: "^(?!login|api).+"
